{
  "name": "@school-mgmt/ai-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --port 8000",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "build": "pip install -r requirements.txt",
    "lint": "ruff check .",
    "test": "pytest"
  }
}